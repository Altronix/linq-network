matrix:
        include:
                - os: linux
                  language: c
                  addons:
                          apt:
                                  packages:
                                          - lcov
                                          - valgrind
                  env: 
                        - WITH_SQLITE=ON
                          ENABLE_TESTING=ON
                          BUILD_EXAMPLES=ON
                  script:
                          - ./scripts/ci/build-c.sh
