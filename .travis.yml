git:
        submodules: false
notifications:
        emails: false
matrix:
        include:
                - os: linux
                  dist: bionic
                  language: c
                  addons:
                          apt:
                                  packages:
                                          - lcov
                                          - valgrind
                  env: 
                          - ENABLE_TESTING=ON
                            BUILD_LINQD=ON
                            USE_SYSTEM_OPENSSL=ON
                  script:
                          - ./scripts/ci/build-c.sh
                - os: linux
                  dist: bionic
                  language: c
                  env:
                          - USE_SYSTEM_OPENSSL=ON
                  script:
                          - ./scripts/ci/build-c.sh
                - os: linux
                  dist: bionic
                  language: c
                  addons:
                          apt:
                                  packages:
                                          - libzmq3-dev
                                          - libczmq-dev
                  env:
                          - USE_SYSTEM_ZMQ=ON
                            BUILD_LINQD=ON
                            USE_SYSTEM_OPENSSL=ON
                  script:
                          - ./scripts/ci/build-c.sh
                - os: linux
                  dist: bionic
                  language: node_js
                  node_js:
                          - 10
                  env:
                          - USE_SYSTEM_OPENSSL=ON
                          - LINQ_NETWORK_WITH_DAEMON=ON
                  script:
                          - ./scripts/ci/build-nodejs.sh
                - os: linux
                  dist: bionic
                  language: node_js
                  node_js:
                          - 10
                  env:
                          - USE_SYSTEM_OPENSSL=ON
                          - LINQ_NETWORK_WITH_DAEMON=OFF
                  script:
                          - ./scripts/ci/build-nodejs.sh
                - os: linux
                  dist: bionic
                  language: rust
                  rust:
                          - nightly
                  env:
                          - USE_SYSTEM_OPENSSL=ON
                  script:
                          - ./scripts/ci/build-rust.sh
                - os: windows
                  language: c
                  env:
                          - LINQ_NETWORK_WITH_DAEMON=OFF
                  script:
                          - ./scripts/ci/build-c.sh
                - os: windows
                  language: node_js
                  node_js:
                          - 10
                  env:
                          - LINQ_NETWORK_WITH_DAEMON=OFF
                  script:
                          - ./scripts/ci/build-nodejs.sh
                - os: windows
                  language: rust
                  rust:
                          - nightly
                  script:
                          - ./scripts/ci/build-rust.sh
